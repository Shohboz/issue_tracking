import React from "react";

export default ({ children }) => (
  <div className="panel-footer">
    <div className="row">
      <div className="col-sm-12 text-center">{children}</div>
    </div>
  </div>
);
